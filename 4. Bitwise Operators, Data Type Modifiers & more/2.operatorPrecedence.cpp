#include <bits/stdc++.h>
using namespace std;

int main()
{
    /*
    unary operators ( !-> ++ -> -- ) -> arithmatic operators( * -> / -> % + -> - ) -> relational operators( < -> <= -> >= -> > -> == -> != ) -> logical operators ( && -> || ) -> assignment operator( = )
    same precedence operators are evaluated from left to right except assignment operator which is evaluated from right to left as well as unary operators.
    */
}